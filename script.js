//your JS code here. If required.
// take input for the first variable and multiply by 2
let firstVar = prompt("Enter the first variable:");
let result = Number(firstVar) * 2;

// take input for the second variable and add to the result
let secondVar = prompt("Enter the second variable:");
result += Number(secondVar);

// display the final result
alert("The final result is: " + result);
